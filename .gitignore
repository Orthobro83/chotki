# personal context — never publish
context.local.md
*.local.md

# swift / spm
.build/
.swiftpm/
*.xcodeproj
*.xcworkspace
DerivedData/

# build output
dist/
*.app/

# spike logs
*.log

# macos
.DS_Store

# Android, from 23 August 2026
android/.gradle/
android/build/
android/*/build/
android/local.properties
android/.idea/
android/.kotlin/

# Heap dumps from a Gradle/dex OOM
*.hprof

# The release signing key and its passwords. Never committed.
keystore.properties
*.jks
*.keystore

# The iOS project is generated from ios/project.yml, which is the reviewable
# source. pbxproj is machine-written, diffs badly, and Xcode rewrites it.
ios/Chotki.xcodeproj/
ios/build/

# The iOS development team. Personal, and a generated project would lose it.
ios/team.env
