package org.chotki.core

/**
 * A placeholder, replaced in phase 2 by [CalendarDate].
 *
 * It exists only to prove the module compiles and its tests run. `PORT.md`
 * phase 1 ends at "CI is green"; nothing here is part of the application.
 */
internal object Chotki {
    const val SPECIFICATION = "core/ in the Swift repository, and its 297 tests"
}
